apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ticketboard

bases:
  - ../../base

patchesStrategicMerge:
  - backend-patch.yaml

images:
  - name: localhost:5000/ticketboard-backend
    newTag: latest
  - name: localhost:5000/ticketboard-frontend
    newTag: latest
